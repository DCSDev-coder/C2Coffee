Options -MultiViews
RewriteEngine On

# Serve the React application for client-side routes while preserving real files.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

RewriteRule ^ index.html [L]
